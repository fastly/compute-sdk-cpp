# This file describes a Fastly Compute package. To learn more visit:
# https://www.fastly.com/documentation/reference/compute/fastly-toml

name = "fastly-cpp-quickstart"
authors = ["you@example.com"]
description = "Quickstart app for the C++ Fastly Compute SDK"
# For now
language = "other"
manifest_version = 3
service_id = ""

[local_server]
backends.fastly.url = "https://www.fastly.com"
backends.wikipedia.url = "https://en.wikipedia.org"

[scripts]
build = "cmake -S . -B build && cmake --build build && cp build/main.wasm bin/main.wasm"
